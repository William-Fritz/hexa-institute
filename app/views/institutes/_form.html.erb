<%= form_with(model: institute, local: true) do |form| %>
  <% if institute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(institute.errors.count, "error") %> prohibited this institute from being saved:</h2>

      <ul>
        <% institute.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :program %>
    <%= form.number_field :program %>
  </div>

  <div class="field">
    <%= form.label :total_student %>
    <%= form.number_field :total_student %>
  </div>

  <div class="field">
    <%= form.label :total_classroom %>
    <%= form.number_field :total_classroom %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
